<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Govi_Aswanna"
        tools:targetApi="31">
        <activity
            android:name=".activities.LoginActivityUM"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SignupActivityUM"></activity>
        <activity android:name=".activities.HomeFarmerActivityUm"></activity>
        <activity android:name=".activities.UserProfileActivityUM"></activity>
        <activity android:name=".activities.UpdateAccountActivityUM"></activity>
        <activity android:name=".activities.HomeBuyerActivityUM"></activity>

        <activity android:name=".activities.FetchingActivity"></activity>
        <activity android:name=".activities.FoodDetailsActivity"></activity>
        <activity android:name=".activities.FoodDetailsBuyerActivity"></activity>
        <activity android:name=".activities.InsertionActivity"></activity>
        <activity android:name=".activities.HomeActivity"></activity>
    </application>

</manifest>